{
  "name": "tank-game",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "devDependencies": {
    "bun-types": "^0.1.8"
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "run": "node dist/index.js",
    "start": "npm run build && npm run run"
  },
  "type": "module",
  "dependencies": {
    "@purplet/gateway": "^1.0.1",
    "@purplet/rest": "^1.0.1",
    "discord-api-types": "^0.37.2"
  }
}